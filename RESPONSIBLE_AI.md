# Responsible AI 

Orbit is a read-only desktop analytics tool that parses GitHub Copilot Chat session logs stored locally by VS Code. It never modifies source logs, never uploads data to external servers, and never collects telemetry. All analysis runs on the user's machine.
